# Response for ppp-failing-test

Read skill [ppp](file:///Users/marcus/.agents/skills/ppp/SKILL.md?vscodeLinkType%3Dskill#1-1), lines 1 to 80

Read skill [ppp](file:///Users/marcus/.agents/skills/ppp/SKILL.md?vscodeLinkType%3Dskill#81-81), lines 81 to 160

**Current phase: Clarifying**

The ticket has placeholder values — I need the actual failing command and error output before I can diagnose anything.

**Blocking questions:**

1. **Command** — what command runs the failing test? (e.g. `npm test`, `pytest path/to/test_foo.py::test_bar`, `go test GitHub.`)
2. **Error output** — the full error or stack trace from that run

Without these two inputs I cannot reproduce, classify, or fix the failure.

Once you provide them I'll move straight to Inspect → classify → patch.